CREATE TABLE CUSTOMER_REWARDS (
    REWARD_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CUSTOMER_EMAIL VARCHAR2(100) REFERENCES CUSTOMERS(EMAIL_ADDRESS),
    GIFT_ID NUMBER REFERENCES GIFT_CATALOG(GIFT_ID),
    REWARD_DATE DATE DEFAULT SYSDATE
);